#############################################################################
# Makefile for building: guest_replay
# Generated by qmake (3.0) (Qt 5.3.0)
# Template: app
#############################################################################

####### Compiler, tools and options

CC            = clang
CXX           = clang++
DEFINES       =
CFLAGS        = -pipe -g -Wall -W -fPIE $(DEFINES)
CXXFLAGS      = -pipe -std=c++11 -g -Wall -W -fPIE $(DEFINES)
CRETE_INC     = ../../lib/include
INCPATH       = -I. -I$(CRETE_INC) -I../include
LINK          = clang++
LFLAGS        =
LIBS          = $(SUBLIBS) -L../bin -lboost_program_options -lboost_filesystem -lboost_serialization -lcrete_test_case -lcrete_vm_comm -lcrete_elf_reader -lcrete_proc_reader -L/usr/local/lib
AR            = ar cqs
RANLIB        =
TAR           = tar -cf
COMPRESS      = gzip -9f
COPY          = cp -f
SED           = sed
COPY_FILE     = cp -f
COPY_DIR      = cp -f -R
STRIP         = strip
INSTALL_FILE  = install -m 644 -p
INSTALL_DIR   = $(COPY_DIR)
INSTALL_PROGRAM = install -m 755 -p
DEL_FILE      = rm -f
SYMLINK       = ln -f -s
DEL_DIR       = rmdir
MOVE          = mv -f
CHK_DIR_EXISTS= test -d
MKDIR         = mkdir -p

####### Output directory

OBJECTS_DIR   = ./

####### Files

SOURCES       = guest_replay.cpp \
		run_config.cpp
OBJECTS       = guest_replay.o \
		run_config.o
DIST          = guest_replay.cpp
DESTDIR       = ../bin#avoid trailing-slash linebreak
TARGET        = $(DESTDIR)/crete-guest-replay
TARGET_INST   = crete-guest-replay


first: all
####### Implicit rules

.SUFFIXES: .o .c .cpp .cc .cxx .C

.cpp.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o "$@" "$<"

.cc.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o "$@" "$<"

.cxx.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o "$@" "$<"

.C.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o "$@" "$<"

.c.o:
	$(CC) -c $(CFLAGS) $(INCPATH) -o "$@" "$<"

####### Build rules

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(LINK) $(LFLAGS) -o $(TARGET) $(OBJECTS) $(OBJCOMP) $(LIBS)

dist:
	@test -d .tmp/guest_replay1.0.0 || mkdir -p .tmp/guest_replay1.0.0
	$(COPY_FILE) --parents $(DIST) .tmp/guest_replay1.0.0/ && (cd `dirname .tmp/guest_replay1.0.0` && $(TAR) guest_replay1.0.0.tar guest_replay1.0.0 && $(COMPRESS) guest_replay1.0.0.tar) && $(MOVE) `dirname .tmp/guest_replay1.0.0`/guest_replay1.0.0.tar.gz . && $(DEL_FILE) -r .tmp/guest_replay1.0.0


clean:compiler_clean
	-$(DEL_FILE) $(OBJECTS)
	-$(DEL_FILE) *~ core *.core


distclean: clean
	-$(DEL_FILE) $(TARGET)


####### Sub-libraries

check: first

compiler_yacc_decl_make_all:
compiler_yacc_decl_clean:
compiler_yacc_impl_make_all:
compiler_yacc_impl_clean:
compiler_lex_make_all:
compiler_lex_clean:
compiler_clean:

####### Compile

main.o: guest_replay.cpp
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o guest_replay.o guest_replay.cpp

####### Install

install: FORCE
	@test -d $(INSTALL_ROOT)/usr/bin || mkdir -p $(INSTALL_ROOT)/usr/bin
	-$(INSTALL_PROGRAM) "$(TARGET)" "$(INSTALL_ROOT)/usr/bin/$(TARGET_INST)"

uninstall: FORCE
	-$(DEL_FILE) "$(INSTALL_ROOT)/usr/bin/$(TARGET_INST)"

FORCE:
